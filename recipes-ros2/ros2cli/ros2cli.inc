# Included by recipe generated by superflore

RDEPENDS_${PN} += "${PYTHON_PN}-xmlrpc ${PYTHON_PN}-pydoc ${PYTHON_PN}-pyparsing ${PYTHON_PN}-pyyaml ${PYTHON_PN}-misc rosidl-generator-py"

FILES_${PN}_prepend = " \
    ${bindir} \
"

do_install_append() {
    rm -rf ${D}${datadir}/${ROS_BPN}/environment
    rm -f ${D}${datadir}/${ROS_BPN}/local_setup.bash
    rm -f ${D}${datadir}/${ROS_BPN}/local_setup.sh
    rm -f ${D}${datadir}/${ROS_BPN}/local_setup.zsh

    rmdir -p --ignore-fail-on-non-empty ${D}${datadir}/${ROS_BPN}
}
