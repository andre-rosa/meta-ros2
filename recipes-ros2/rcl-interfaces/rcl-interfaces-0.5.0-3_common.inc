# Included by recipe generated by superflore

# Without the target rosidl_typesupport_{c,cpp}, ament finds the native packages and then fails to link (error: incompatible
# target).
DEPENDS += "rosidl-typesupport-c rosidl-typesupport-cpp"
